<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@android:color/white"
    tools:context=".SiteDetailActivity">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="32dp">

        <!-- Flecha Atrás -->
        <ImageView
            android:id="@+id/backButton"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="24dp"
            android:layout_marginTop="24dp"
            android:src="@drawable/ic_arrow_back_custom"
            android:contentDescription="Volver"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Título Principal -->
        <TextView
            android:id="@+id/detailTitleTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:layout_marginHorizontal="24dp"
            android:text="Basílica Católica Nuestra Señora de la Merced | Quito"
            android:textColor="#1A2E35"
            android:textSize="26sp"
            android:textStyle="bold"
            android:lineSpacingExtra="4dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/backButton" />

        <!-- Línea separadora debajo del título -->
        <View
            android:id="@+id/titleDivider"
            android:layout_width="0dp"
            android:layout_height="2dp"
            android:layout_marginTop="8dp"
            android:background="#1A2E35"
            app:layout_constraintEnd_toEndOf="@+id/detailTitleTextView"
            app:layout_constraintStart_toStartOf="@+id/detailTitleTextView"
            app:layout_constraintTop_toBottomOf="@+id/detailTitleTextView" />

        <!-- Imagen Principal -->
        <androidx.cardview.widget.CardView
            android:id="@+id/imageContainer"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:layout_marginHorizontal="24dp"
            android:layout_marginTop="32dp"
            app:cardCornerRadius="0dp"
            app:cardElevation="0dp"
            app:layout_constraintTop_toBottomOf="@+id/titleDivider">

            <ImageView
                android:id="@+id/detailImageView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:src="@drawable/placeholder"
                android:background="#EEE" />
        </androidx.cardview.widget.CardView>

        <!-- Pestañas (Tabs) personalizadas -->
        <LinearLayout
            android:id="@+id/tabsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="24dp"
            android:layout_marginTop="24dp"
            android:orientation="horizontal"
            app:layout_constraintTop_toBottomOf="@+id/imageContainer">

            <!-- Tab Acerca de -->
            <LinearLayout
                android:id="@+id/tabAboutContainer"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal">

                <TextView
                    android:id="@+id/tabAboutText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Acerca de"
                    android:textColor="#1A2E35"
                    android:textStyle="bold"
                    android:textSize="16sp"
                    android:paddingBottom="8dp"/>

                <!-- Indicador de selección (línea gruesa) -->
                <View
                    android:id="@+id/tabAboutIndicator"
                    android:layout_width="80dp"
                    android:layout_height="4dp"
                    android:background="#455A64"
                    android:visibility="visible"/>
            </LinearLayout>

            <!-- Tab Información Adicional -->
            <LinearLayout
                android:id="@+id/tabInfoContainer"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center_horizontal">

                <TextView
                    android:id="@+id/tabInfoText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Información Adicional"
                    android:textColor="#757575"
                    android:textSize="16sp"
                    android:paddingBottom="8dp"/>

                <!-- Indicador invisible por defecto -->
                <View
                    android:id="@+id/tabInfoIndicator"
                    android:layout_width="match_parent"
                    android:layout_height="4dp"
                    android:background="#455A64"
                    android:visibility="invisible"/>
            </LinearLayout>
        </LinearLayout>

        <!-- Línea gris suave debajo de las tabs -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginHorizontal="24dp"
            android:background="#E0E0E0"
            app:layout_constraintTop_toBottomOf="@+id/tabsContainer" />

        <!-- Texto descriptivo -->
        <TextView
            android:id="@+id/descriptionTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Descripción del sitio..."
            android:textColor="#000000"
            android:textSize="14sp"
            android:lineSpacingMultiplier="1.2"
            android:justificationMode="inter_word"
            app:layout_constraintEnd_toEndOf="@+id/detailTitleTextView"
            app:layout_constraintStart_toStartOf="@+id/detailTitleTextView"
            app:layout_constraintTop_toBottomOf="@+id/tabsContainer"
            tools:targetApi="o" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>